class LandingModule {
    // Exercise 6: Get location updates (throttle to get a precise distance to destination=,
    // distance must be less than the limit to land (throttle, map, combine) and landing team must allow landing
}